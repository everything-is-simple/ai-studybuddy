# PHASE3-T02F：运行日志脱敏、轮转与保留/清理安全边界——实施计划

**状态**：计划已创建，待审查/待用户批准实施
**计划日期**：2026-07-26
**计划分支**：`codex/phase3-t02f-log-redaction-rotation-retention-plan`
**前置事实**：`origin/master` 已包含并推送 T02E 计划提交 `4b86ddd`、实施提交 `21680a06` 与主线复验登记 `7001fba`。T02E 切片完成不等于 PHASE3-T02 总体、Phase 3、安全与隐私基线审计、生产上线或用户电脑验收完成；PHASE3-T02 总体审计仍在进行中。
**本计划性质**：本轮仅建立和审查后续实施边界；未实施日志保护，未修改业务代码、部署脚本、测试代码、配置、日志实现或基础设施代码，未执行真实日志轮转、清理、递归删除、部署流程、打包、扫描或网络调用。

---

## 1. 目标与唯一范围

本切片只为未来实施建立“运行日志脱敏、轮转与保留/清理安全边界”。用户单独批准后，后续实施唯一可以覆盖：

1. 审计并约束运行日志、错误日志、部署/运维日志及其可清理文件的受控日志根、允许子目录与明确 allowlist；
2. 定义日志字段、异常消息、子进程输出、请求/响应元数据及诊断信息的脱敏原则，禁止记录真实秘密、令牌、密码、Cookie、环境变量、真实学生资料、完整文件内容或不必要的个人信息；
3. 对未来轮转、保留与清理建立路径规范化、allowlist、越界拒绝、空路径拒绝、仓库根/磁盘根/用户目录拒绝与重解析点风险处理的 fail-closed 契约；
4. 让未来的错误输出只使用固定、脱敏、非绝对路径的错误码或分类，不回显完整宿主路径、运行环境、stack、原始命令输出、环境变量值或文件内容；
5. 仅使用仓库外、无真实数据的合成隔离夹具和明确隔离的测试日志根证明上述边界。

本计划不声明现有日志实现已经满足该契约，也不把计划、未来分支验证、未来主线集成或未来推送描述为 T02、Phase 3、安全审计、生产上线或用户电脑验收完成。

## 2. 已核验的只读起点

- 当前本地 `master` 与 `origin/master` 在本计划创建前关系为 `0 0`，共同指向 `7001fba`；主仓工作树干净。相关历史 worktree 已仅作只读状态核对，未覆盖、重置、清理、删除或移动任何内容。
- `docs/08-共同底座架构-Architecture.md` 规定运行目录含 `logs/`；`docs/13-部署运维指南-Deployment.md` 把 `logs/` 作为正式运行目录的脱敏运行日志并从备份白名单排除；`docs/10-后端开发规范-Backend-Guidelines.md` 规定运行日志不进入 Git，且 AI Router 日志仅允许受限的业务摘要字段。
- 只读审计发现的后续候选包括：`packages/backend/src/utils/ai-logger.ts` 目前将 JSON 事件写到 `console.log`；`packages/backend/src/db/semester-initializer.ts` 在维护失败时向 `APP_DATA_ROOT/logs/semester-init-failures.jsonl` 追加原始错误消息；`packages/backend/src/db/paths.ts`、`packages/backend/src/config/env.ts`、`packages/backend/src/server.ts`、`packages/backend/src/scripts/parent-report-runner.ts` 与部署 PowerShell wrapper 可能影响运行根、错误传播或运维输出。
- 上述观察只用于界定未来审计对象，**不是对现状的认可、实现结论、真实日志读取、真实日志处理或脚本执行**。本轮未读取真实日志、真实数据、真实环境文件或用户目录。

## 3. 明确分离的验证类别

### 3.1 本计划及未来可预先审查的“静态审计和合成隔离夹具验证”

- 静态审查候选日志调用、写入点、异常处理和部署/运维输出点；
- 在仓库外临时目录创建无真实数据的合成日志根、受保护根和哨兵文件；
- 使用合成秘密、合成路径、合成异常、合成子进程 stdout/stderr 和合成请求/响应元数据验证脱敏与拒绝条件；
- 仅对未来明确隔离的测试日志根做模拟轮转/保留/清理断言，且测试前后检查受保护哨兵未被读写或删除。

### 3.2 严格禁止的“真实操作”

本轮及未获单独批准的后续工作都**不得**执行真实日志轮转、真实日志清理、真实递归删除、真实服务运行或真实部署流程；也不得把真实用户数据、真实日志、生产日志、正式数据、外部 worktree、真实 `APP_DATA_ROOT` 或用户目录作为测试对象。真实打包、真实秘密扫描、真实部署包扫描、Provider、SMTP、Webhook、OCR、whisper.cpp、网络服务调用与任何破坏性命令同样禁止。

## 4. 未来实施安全契约

### 4.1 受控日志根、允许子目录与 allowlist

未来实现必须在任何新建、追加、枚举、压缩、轮转、保留判断或清理之前，规范化每一个日志候选路径并建立完整路径段关系（不得使用宽松字符串前缀）：

- **日志根**只能是未来实现显式配置并校验的受控日志根；不得隐式复用仓库根、应用数据根或任意当前工作目录。现有文档中日志拓扑与 `APP_DATA_ROOT` 约定存在交叉时，实施前必须先通过最小化、已批准的规范对齐确定唯一受控根，不能以运行时猜测扩大范围。
- **允许子目录与文件类别**必须是显式、有限、不可由任意外部输入扩展的 allowlist；只能处理实现时定义的运行日志、错误日志、部署/运维日志及其轮转副本。未知目录、未知文件类别与非日志数据默认拒绝。
- **可清理目标**必须同时证明：位于规范化日志根下、属于 allowlist、是可清理的日志类别，并满足未来实施定义的保留策略；日志根本身、其父目录和任何预存/未知目录不得作为递归清理目标。
- 空值、空白、相对路径、无法规范化的路径、`..` 逃逸、仓库根、磁盘/卷根、用户目录、外部 worktree、`APP_DATA_ROOT`、正式数据库、正式数据、备份、模型、部署包输出、staging 目录及其他非日志数据一律拒绝。
- 枚举、轮转与清理前后必须识别符号链接、junction、mount point 和其他重解析点及其解析目标；默认不跟随、不作为处理目标。若无法可靠判断，必须以固定错误类别拒绝，不能降级为递归处理。

### 4.2 内容脱敏与最小记录原则

未来实现必须以字段 allowlist 为基线，而不是尝试记录完整对象后再事后过滤：

- 日志字段只保留为诊断所必需的固定事件类型、稳定错误码/分类、受限操作类别、非敏感短标识、时间、耗时和允许的状态摘要；实际字段表须在实施时固定并受测试约束。
- API Key、token、密码、Cookie、完整 Webhook、Provider URL、环境变量值、完整宿主环境、真实学生资料、资料/笔记/答案全文、完整文件内容、原始请求/响应体、完整 UUID、真实路径及外部服务原始响应不得记录。
- 异常消息、`cause` 链、stack、子进程 stdout/stderr、原始命令输出与诊断对象必须默认视为不可信输入；只能归类为固定脱敏码/摘要，不得原样拼入日志或公开错误。
- 请求/响应元数据只允许经明确字段 allowlist 的非敏感最小摘要；不得以调试、失败重试、运维或测试便利为由记录正文、头、Cookie、认证信息、文件内容或个人信息。
- 任何脱敏失败、不确定字段或无法序列化的诊断信息必须 fail closed：记录固定类别而不是回退到 `String(error)`、对象 dump、环境 dump 或原始输出。

### 4.3 轮转、保留与清理拒绝/错误契约

未来实现的操作前置条件与错误输出必须可测试：

- 只有已规范化并通过日志根/allowlist/非重解析点校验的合成测试日志文件，才能成为未来轮转、保留或清理的候选；其他目标必须在递归枚举、删除或内容读取之前拒绝。
- 未来固定错误分类可采用 `LOG_ROOT_INVALID`、`LOG_TARGET_EMPTY`、`LOG_TARGET_OUTSIDE_ALLOWLIST`、`LOG_TARGET_PROTECTED_ROOT`、`LOG_TARGET_REPARSE_POINT`、`LOG_RETENTION_INVALID`、`LOG_CONTENT_REDACTED` 等形式；具体命名待实施时统一，但不得携带绝对路径、环境、stack、命令输出、文件内容或秘密。
- 面向调用者、控制台和测试断言的错误消息只能返回固定、脱敏、非绝对路径的错误码/类别；内部诊断也不得通过另一日志通道回显被禁止的原始值。

## 5. 候选/待实施文件与测试类别

以下均为**候选/待实施**对象，不代表已修改、已验证或将必然修改：

| 类别 | 候选/待实施文件或位置 | 未来目的 |
| --- | --- | --- |
| 日志抽象 | `packages/backend/src/utils/ai-logger.ts`（候选） | 将 AI 事件的字段 allowlist、异常摘要与输出目的地纳入受控契约。 |
| 维护失败日志 | `packages/backend/src/db/semester-initializer.ts`（候选） | 审计 JSONL 原始错误消息和清理错误消息的脱敏边界。 |
| 运行路径 | `packages/backend/src/db/paths.ts`、`packages/backend/src/config/env.ts`（候选） | 定义日志根配置、路径规范化与受保护根比较的可复用边界。 |
| 启动/运维输出 | `packages/backend/src/server.ts`、`packages/backend/src/scripts/parent-report-runner.ts`、`scripts/*.ps1`、`scripts/lib/AIStudyBuddy.Deployment.psm1`（候选，只读审计起点） | 审计启动、部署和计划任务错误的最小化输出；不处理部署包输出或 staging 删除边界。 |
| 后端测试 | `packages/backend/test/log-redaction-rotation-retention-boundary.test.mjs`（候选新增） | 覆盖日志字段、异常/子进程输出、路径与危险目标拒绝。 |
| PowerShell 测试 | `scripts/test-log-redaction-rotation-retention-boundary.ps1`（候选新增） | 仅用仓库外合成夹具覆盖部署/运维日志路径与错误输出契约（如实施确有 PowerShell 范围）。 |
| 相邻回归 | `packages/backend/test/ai-router.test.mjs`、`packages/backend/test/semester-initialization.test.mjs`、`packages/backend/test/env-error-redaction.test.mjs`、`packages/backend/test/subprocess-environment-boundary.test.mjs`（候选） | 保持既有日志/异常/子进程脱敏约束，并添加最小必要回归。 |

未来自动化测试至少应包括：

1. 静态日志调用审计；
2. 敏感字段与异常/子进程输出脱敏测试；
3. 日志根 allowlist 与路径规范化测试；
4. 危险路径拒绝测试；
5. 仓库外合成隔离夹具测试；
6. 错误输出不泄露绝对路径、环境、stack、命令输出或文件内容的测试；
7. 轮转/保留候选的 allowlist、非日志文件拒绝与不伤害哨兵测试。

## 6. 隔离夹具与未来验收标准

### 6.1 强制隔离约束

- 所有未来验证只能在仓库外临时目录、无真实数据的合成夹具和明确隔离的测试日志根中进行；目录应使用每次运行唯一标识，并在测试断言中与仓库、用户目录、`APP_DATA_ROOT`、外部 worktree 和正式运行目录分离。
- 夹具只能包含合成日志条目、合成秘密/个人信息哨兵、合成路径、合成错误、合成 stdout/stderr 与合成非日志受保护哨兵；禁止复制、读取或引用真实日志、真实资料、正式数据库、备份、模型或环境文件。
- 若平台无法安全创建、检查或隔离重解析点夹具，相关测试必须显式报告跳过理由或拒绝行为，不能以真实路径替代。

### 6.2 未来通过条件

仅在用户另行批准实施后，且下列条件都满足时，才可报告为“分支实现完成，待主线集成/复验”（不是本计划阶段的状态）：

- 静态与合成测试证明所有日志输出遵守字段 allowlist，合成秘密、路径、文件内容、环境值、stack、命令输出与个人信息均不会泄露；
- 危险输入、越界路径、空路径、根路径、仓库根、用户目录、`APP_DATA_ROOT`、重解析点/符号链接风险路径和非 allowlist 路径全部在任何递归枚举、轮转或清理前被拒绝；
- 合成成功路径只影响明确隔离测试日志根下、属于 allowlist 的合成日志文件；仓库与全部受保护哨兵在测试前后不变；
- 轮转、保留与清理错误都只含固定、脱敏、非绝对路径的错误码/分类；
- 仅执行经批准的针对性测试、文档治理和 `git diff --check`，并如实记录命令、退出状态、隔离根类别与限制。

### 6.3 未来失败/停止条件

出现任一情况必须拒绝目标或停止实施，不得“先处理、后补保护”：

- 目标为空、无法规范化、不是 allowlist 日志目标、等于/包含/被包含于受保护根，或不能证明其属于隔离日志根；
- 发现或无法可靠检查符号链接、junction、mount point 或其他重解析点；
- 验证需要真实日志、真实服务、真实部署流程、真实用户数据、真实 `APP_DATA_ROOT`、外部 worktree 或任何正式数据；
- 输出可能泄露绝对路径、真实文件内容、运行环境、stack、原始命令输出、环境变量值、秘密或个人信息；
- 需求扩展至备份/恢复、ACL、部署包输出、staging 删除边界、生产防火墙、Docker/WSL、S7、G2、S3 Worker、用户电脑验收或 T02 总体收尾。

## 7. 风险与回滚边界

- 本计划阶段没有运行时、部署、日志或数据变更，因此没有真实轮转回滚、清理回滚、部署回滚或数据恢复操作。
- 未来实施必须 fail closed；不能以 `-Force`、忽略错误、宽松路径前缀、默认目录、隐式清理或“日志应可删除”的假设绕过保护。
- 未来测试失败时，只允许保留最小化、无真实数据的隔离夹具以供诊断；夹具本身的后续处置也必须先满足已批准的边界，绝不能借此处理仓库、用户目录、正式运行目录或外部 worktree。
- T02E 已完成的部署包输出与 staging 删除边界不属于本任务；T02F 不得重复、削弱、改变或将其作为日志清理的扩展入口。

## 8. 明确非范围

本计划及本轮均不处理：

- `package`、`bootstrap`、`check`、`start`、`health`、`stop`、`backup`、`restore` 脚本修改，或任何业务代码、测试代码、配置、日志实现或基础设施代码修改；
- 真实日志脱敏、日志轮转、日志保留、日志清理保护、真实递归删除或真实服务/部署流程；
- 备份/恢复、ACL、部署包输出、staging 删除边界、生产防火墙、Docker/WSL；
- S7、G2、S3 Worker、用户电脑验收、生产上线、PHASE3-T02 总体或 Phase 3 总体收尾；
- 真实秘密扫描、真实部署包打包、真实部署包扫描或任何破坏性命令；
- Provider、SMTP、Webhook、OCR、whisper.cpp 或网络服务调用；
- 创建未来 PRD、路线图或扩大任务范围。

## 9. 计划阶段独立审查清单与结论

提交前必须独立检查：

- 范围是否只覆盖运行日志脱敏、轮转与保留/清理边界；
- 是否明确禁止真实日志清理、真实轮转和真实递归删除；
- 是否具有可测试的拒绝条件与仓库外合成隔离夹具约束；
- 是否避免泄露绝对路径、真实文件内容、运行环境、stack、原始命令输出或秘密；
- 是否明确排除 T02E 的部署包输出/staging 删除范围；
- 非范围、状态措辞和 `docs/04` 索引登记是否准确一致；
- 是否没有把 T02F 写成 T02 总体、Phase 3、安全审计、上线或用户电脑验收完成。

**独立审查结论：通过。** 本计划仅定义运行日志脱敏、轮转与保留/清理的未来边界；真实日志轮转、真实清理和真实递归删除均被明确禁止。拒绝条件、隔离夹具、脱敏输出和 T02E 排除边界可供未来实施测试；候选文件均标识为待实施，`docs/04` 仅登记“计划已创建，待审查/待用户批准实施”。此结论只证明计划边界与状态登记完整，不证明任何日志保护已经实施、验证、合入或推送。
