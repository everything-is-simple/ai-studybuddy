# PHASE3-T02-R1 正式仓库与候选部署包秘密扫描签收实施计划

**状态**：📝 已创建，待独立审查与单项实施批准。用户已于 2026-07-28 批准按 `PHASE3-REOPEN-HIGH-WEIGHT-20260728` 从 Wave 0 开始；该批准允许创建本逐项计划与计划分支，不代替本计划的独立审查或真实扫描签收。

**任务**：T02-R1：正式仓库与实际候选部署包的秘密扫描签收。

**上位计划**：`PHASE3-REOPEN-HIGH-WEIGHT-20260728` Wave 0。

**前置事实**：T02D 已在 `origin/master` 建立确定性扫描库与仓库外合成夹具证明；它不是真实仓库或真实部署包扫描。T02-R1 仅在以下计划门禁、独立审查和明确单项批准全部满足后，才可对批准输入产生真实签收结论。T02、Phase 3、生产上线与用户电脑验收均仍未完成。

## 1. 唯一目标与完成口径

在不泄露秘密、内容、绝对路径、敏感相对文件名或原始终端输出的前提下，对两类**显式、可审计输入**执行一次确定性扫描并形成脱敏签收：

1. 当前提交的 Git 已跟踪文件清单；
2. 经人工批准的、单一、已识别的候选部署包目录根。

只有两类输入均已实际完成、无 P0 或 P0 已按条件流程闭合，且证据字段满足第 4 节，T02-R1 才可标记完成。仅完成脚本、单元测试、仓库扫描或合成包扫描均不构成 R1 完成。

## 2. 不可逾越的输入与停止门

### 2.1 Git 跟踪文件

- 唯一来源是目标提交工作树内 `git ls-files -z` 的结果；不得递归枚举仓库根、工作区、用户目录或磁盘根。
- 清单在扫描前记录提交短哈希、文件计数和扫描器版本；任何未跟踪、忽略、仓库外、子模块外或无法归一化的条目均不在候选集。
- `.env.local`、`production.env`、超限文件、非文本文件、符号链接/reparse point 均由既有安全库在内容读取前跳过或拒绝；跳过只以聚合计数证明。

### 2.2 候选部署包

- 必须由用户或受权审查者在单项批准中提供：artifact ID、构建提交短哈希、包 SHA-256 短指纹、明确的逻辑“候选包目录根”及批准时间窗口；不得由脚本搜索、猜测或挑选包。
- 本任务只接受可访问的目录根，不接受 zip 自动解压、网络位置、用户数据根、运行中安装根、备份根、日志根或临时根。目录清单仅在该经批准根内以 fail-closed 方式生成；reparse point、敏感环境文件、非文本文件和超限文件不读取。
- 候选包的构建提交必须与批准记录可比对；若不一致、路径不可验证、根外解析、manifest 缺失/不一致或窗口过期，输出固定失败码并停止，不产生“通过”结论。

### 2.3 绝对禁止

- 不扫描整个工作区；不读取、枚举、移动、删除或提交 `H:\ai-studybuddy\.trae-html-share-packages\`、`H:\ai-studybuddy\alpha-sprint-plan\` 或任一其他未跟踪目录。
- 不扫描真实环境文件、`APP_DATA_ROOT`、正式数据、学生资料原文、日志、备份、模型、外部 worktree、用户目录或运行时安装目录。
- 不调用 Provider、SMTP、Webhook、OCR、whisper.cpp、网络服务；不修改 ACL、备份/恢复、业务 Schema/API/前端或部署运行行为。

## 3. 拟议最小实现

1. 在 `scripts/lib/AIStudyBuddy.SecretScan.cjs` 上扩展**仅聚合、不可回显路径或命中内容**的签收摘要构造器；保留 T02D 既有 `scanSecretBoundary` 输入拒绝与允许字段契约，不降低其边界。
2. 新增一个显式参数的 PowerShell 签收入口（建议 `scripts/confirm-secret-scan-signoff.ps1`）：
   - 只有显式 `-PackageRoot`、artifact 元数据与批准窗口同时存在时才扫描候选包；
   - 使用 `git ls-files -z` 取得仓库候选清单，候选包只从已批准根读取；
   - 输出/可持久化 JSON 只含扫描器版本、artifact ID、构建短哈希、包短指纹、批准窗口标识、两类输入的扫描/跳过/命中聚合计数、稳定规则 ID/类别的聚合计数、结果码和时间戳；不含路径、文件名、行号、完整 hash、内容、环境变量、PowerShell 命令或绝对位置；
   - 缺少批准参数、候选包不符合边界、输入访问失败或任何命中时 fail-closed；命中只能报告 `T02_P0_SECRET_SCAN_HIT` 及类别化统计，立即阻止普通 Wave 继续。
3. 新增 `packages/backend/test/secret-scan-signoff.test.mjs`，并只在 `packages/backend/test/secret-scan-boundary.test.mjs` 需要验证兼容性处做最小补充；全部夹具必须位于仓库外隔离临时根，使用无效合成哨兵。
4. 仅在审查、批准和实际输入齐备后，运行一次真实签收；实际证据不得提交入仓。可提交的 `docs/04` 只登记脱敏摘要、执行提交、artifact ID、短指纹、批准窗口和退出结论。

## 4. 脱敏证据契约与 P0 处置

允许在控制台摘要、CI/测试结果及最终任务登记中出现的仅限：固定结果码、扫描器版本、提交短哈希、artifact ID、包 SHA-256 短指纹、批准窗口标识、聚合数量、稳定规则 ID/类别聚合和通过/阻止结论。

一律禁止出现：API Key、命中原文、完整配置行、文件内容、相对路径/文件名、绝对路径、完整 SHA-256、完整 UUID、完整 SID、环境变量行、manifest 原文、原始终端 transcript、PowerShell 原始命令、堆栈或底层 IO/Git 错误。

任一真实命中、疑似正式秘密、输入边界不可验证或脱敏契约破坏均视为普通 Wave 的停止条件：不打印更多细节、不继续 R2/T04/T05，登记不含定位信息的 `T02_P0_SECRET_SCAN_HIT` 或固定边界错误，由用户决定受控取证与 T02-P0 应急流程。

## 5. 测试与验收

实现阶段至少应证明：

- Git 输入只能来自已跟踪清单，未跟踪文件和敏感 env 文件在读取前被排除；
- 仅有显式、批准的候选包目录根才会被扫描；缺少/越界/无 manifest/不匹配元数据全部 fail-closed 且不回显路径；
- reparse、超限和非文本候选不读内容，只体现在聚合跳过计数；
- 命中和所有失败路径的 JSON、异常、格式化文本、断言消息均不含动态合成哨兵；
- 可持久化签收摘要不含路径、文件名、行号、完整 hash 或命中原文；
- 实现验证至少运行 `node --test packages/backend/test/secret-scan-boundary.test.mjs packages/backend/test/secret-scan-signoff.test.mjs`、`pnpm type-check`、backend build、frontend build、设置隔离 `APP_DATA_ROOT` 的 `pnpm test`、文档治理与 `git diff --check`。

真实签收额外要求：两个批准输入均成功完成、输出仅含第 4 节字段、无 P0 且候选包身份与批准记录一致；否则保持 T02-R1 未完成。

## 6. 审查清单与后续动作

独立审查必须确认：

- T02D 合成夹具边界没有被误写为真实扫描结论；
- 任意候选包均需显式人工批准，脚本没有搜索或隐式递归工作区；
- 未跟踪目录与正式数据根不会被读取；
- P0 命中与边界错误均 fail-closed，且不把定位或命中值写入可观察面；
- 新增脚本不会改变部署、启动、备份、恢复或 ACL 行为；
- 文档状态只表述“计划/实现/签收”的实际层级，不提前完成 T02 或 Phase 3。

审查通过后，请用户明确回复：`批准按 PHASE3-T02-R1-REAL-SECRET-SCAN-20260728 实施`。只有收到该单项批准后，才可实现脚本、运行合成验证或接收真实候选包审批信息；真实扫描本身仍须在输入元数据齐备时单独执行。
