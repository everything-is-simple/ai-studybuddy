# Phase 4：系统验收收口——独立实施计划

**计划编号**：PHASE4-ACCEPTANCE-20260806
**状态**：📝 已创建；待独立审查与用户确认后实施（按 16 步工作流）
**日期**：2026-08-06
**任务分支**：`claude/phase4-acceptance-20260806`
**上游**：`docs/09-测试验收计划-Test-Plan.md`（历史验收记录）、`docs/13-部署运维指南-Deployment.md`（部署验收）、各子系统 PRD、Phase 3 Wave 0-3 证据

---

## 1. 问题与目标

### 1.1 现状问题

`docs/09-测试验收计划-Test-Plan.md` 是**按 Phase 历史堆积的验收记录**（0.5→0.7→0.8→1→2→S7），**没有"当前 v0.8.1 全系统验收清单"**——即没有回答"现在这份代码，验收什么、每项标准是什么、是否通过"的统一基线。

同时存在**文档滞后偏差**（AUDIT-20260806 已修代码侧，文档侧未全同步）：
- docs/09 §12 备份/恢复仍写"非 `-WhatIf` 固定拒绝 `RESTORE_WRITE_DISABLED`"——与 Wave 1 已实现的受控恢复写入（`-EnableWrite` 8 步状态机）矛盾
- docs/09 状态行 G2/Phase 1.5 描述过时
- docs/09 无 Phase 3 Wave 0-3 验收记录

### 1.2 目标

建立**当前版本的确定验收基线**：一份"v0.8.1 全系统验收矩阵"，使验收有单一、确定、可勾选的标准来源，替代历史堆积；同步修正 docs/09 滞后项。

## 2. 范围

### 2.1 验收矩阵结构（新增 docs/09 章节"当前版本验收矩阵"）

| 维度 | 验收项 | 通过标准 | 当前证据 |
|---|---|---|---|
| **S1 学习节奏** | 学期创建/切换/隔离、课程、考试确认、任务、时间线 | 学生主路径可用 | 后端测试 + E2E semester-selector/course-schedule/exam-workbench/timeline |
| **S2 资料笔记** | 上传/转换/AI 降级/笔记渲染/知识模块 | 资料闭环可用 | 后端测试 + E2E student-journey + 真实业务冒烟 |
| **S3 限时练习** | 生成/作答/批改/历史 | 练习闭环可用 | 后端测试 + E2E practice-runner + T02-R4 错误矩阵 |
| **S4 错题改错** | 归档/错因/重做/薄弱点 | 改错闭环可用 | 后端测试 + E2E error-fixer |
| **S5 期末冲刺** | 模拟考/速背/冲刺计划/工作台 | 冲刺闭环可用 | 后端测试 + E2E mock-exam/cram-cards/cram-plan + T02-R4 |
| **S6 家长报告** | 规则报告/AI 降级/渠道去重 | 脱敏聚合正确 | 后端测试 parent-report* |
| **S7 课堂采集** | 受控 WAV/同步转写/S2 handoff | MVP 边界可用 | S7-MVP 验收 + T02-R4 |
| **部署** | 部署包/Bootstrap/启停/OCR/备份恢复/配置/任务计划/安全网络 | docs/13 §12 8 项 | 部署矩阵 + Wave 0-3 证据 |
| **安全基线** | 秘密扫描/ACL/日志脱敏/错误矩阵/备份恢复状态机 | Phase 3 Wave 0-3 闭合 | R1/R2/R4/T04/T05 证据 |
| **全量自动化** | type-check/build/test/E2E | 全绿 | 342 后端 + 149 前端 + 24 E2E |

### 2.2 文档同步修正

1. docs/09 §12 备份/恢复：`RESTORE_WRITE_DISABLED` → 受控恢复写入（`-EnableWrite` 8 步状态机，Wave 1/2 已实现并演练）
2. docs/09 状态行：G2/Phase 1.5 状态更新
3. docs/09 新增 Phase 3 Wave 0-3 验收记录
4. docs/00 索引同步

## 3. 非目标

- 不实现新功能；仅建立验收标准与证据基线。
- 不运行真实外部服务（AI/SMTP/飞书/真实渠道）。
- 不做目标用户电脑验收（那是独立门禁）。
- 不宣称 Phase 3 或生产上线完成。

## 4. 实施步骤

1. 登记 docs/04 Phase 4 任务。
2. 编写 docs/09"当前版本验收矩阵"章节（引用已有证据，不虚构）。
3. 修正 docs/09 滞后项（备份恢复/G2/Phase 3 记录）。
4. 更新 docs/00 索引。
5. 治理检查 + diff 检查。
6. 提交 → 合入 master → 推送 origin/master。

## 5. 验证

- `scripts/check-docs-governance.ps1` 通过。
- `git diff --check` 通过。
- 验收矩阵每项引用真实证据路径（测试文件/E2E/证据 JSON）。
- 全量测试基线（342/149/24）在验收矩阵中如实呈现，不夸大。

## 6. 交付

- docs/09 新章节 + 同步修正 + docs/04 登记 + docs/00 更新。
- 交付说明列出：验收矩阵结构、每项证据来源、修正的滞后项、未完成边界。
