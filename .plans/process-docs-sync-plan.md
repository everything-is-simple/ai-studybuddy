# DOCS-20260718 系统文档一致性行动计划

**日期**：2026-07-18  
**分支**：`codex/process-docs-sync`  
**状态**：用户已明确要求实施，执行中

## 1. 目标

1. 以 `origin/master` 当前代码、测试和 `docs/04` 完成证据为事实源，消除仍把 T03/T04/T06/T08 写成未实现或候选的陈旧表述。
2. 让 `docs/04` 明确展示 T09A–T09E 路线，并把 T09A 固定为下一门禁。
3. 固化每个任务必须经过“先登记、再 `.plans/` 计划、审查批准、实现验证、更新 `docs/04`、合入并推送”的可审计流程。

## 2. 更新范围

- 任务与入口：`docs/00`、`docs/04`、`docs/07`、`docs/12`、`AGENTS.md`、`CLAUDE.md`。
- 产品与子系统状态：`docs/01`、`docs/02`、S1/S2/S3/S4/S6 PRD。
- 技术与验收事实：`docs/05`、`docs/06`、`docs/08`、`docs/09`、`docs/10`、`docs/11`。
- 前端路线：`docs/15`，同步设置页已实现、当前路由数和 T09A–T09E 分工。

## 3. 事实边界

- T08 已在 `origin/master` 实现并验证；真实 AI、SMTP、飞书 smoke 未在常规验收中执行。
- T09A–T09E 仅登记路线，尚无实施完成事实；本任务不创建 T09A 实施计划，不写 T09 业务代码。
- S5、S7、Phase 3 不提前触发；不创建未到门禁的 PRD。
- 不为制造修改记录而改写无关产品需求；每个文件只同步与其职责相关的状态、契约或流程。

## 4. 验证

1. 搜索所有有效文档，确认不存在“T08 尚未实现/下一门禁 T08”“S3 业务代码尚未开始”“S4 未创建”等陈旧当前态。
2. 确认 `docs/04` 同时包含 T09A–T09E、计划路径、状态语义和本任务完成证据。
3. 运行 `scripts/check-docs-governance.ps1`。
4. 运行 `git diff --check`，并审查只修改计划内文档。
5. 提交任务分支，按规则快进合入最新 `master`，在 master 复验后推送 `origin/master`。
