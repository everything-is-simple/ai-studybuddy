# Phase 3 高权重必做项恢复计划

**计划编号**：PHASE3-REOPEN-HIGH-WEIGHT-20260728
**状态**：📝 已创建，等待独立审查与用户对本计划的实施确认；未开始业务实现
**创建日期**：2026-07-28
**任务分支**：`codex/phase3-reopen-high-weight-plan`（仅计划和任务清单同步）
**授权背景**：用户于 2026-07-28 明确撤销 2026-07-27 的 Phase 3 全面暂停决定，要求仅恢复“必须做且高权重”的小任务。该授权不等于批准本计划中各实施切片的具体变更；独立审查后须取得本计划的明确实施确认。

---

## 1. 目标与范围

目标是完成上线/真实使用前不可省略的 P1 安全、隐私、数据可恢复性、日志、学生核心失败反馈和安全总体验收工作，形成真实 Windows 原生证据；不以合成夹具、开发机候选包或单项提交外推为 Phase 3、生产上线或用户电脑验收完成。

本计划的创建、审查和文档修订均不读取真实秘密、正式数据或目标 Windows 机器，因此不能将“计划审查未确认 P0”表述为真实环境 `P0=0`。只有完成已批准范围的 R1/R2 实际证据、复核与处置后，才可对该范围作 P0 结论；一旦 R1 或 R2 确认秘密/正式数据泄露或可直接破坏活动数据边界的 P0，必须停止普通 Wave 并进入条件触发的 T02-P0 处置。

### 本轮纳入（11 个必做小任务）

| Wave | 任务 | 高权重理由 | 主要前置 |
| ---- | ---- | ---------- | -------- |
| 0 | T02-R1 正式仓库与实际部署包秘密扫描签收 | 秘密泄露为 P0；正式扫描是上线前 P1 门禁 | 已有 T02D 合成扫描边界、实际候选包 |
| 0 | T02-R2 真实 Windows 目录与 ACL 证据 | 数据/配置/日志/备份目录越权是 P1 | 已有 T02G 只读 ACL 契约；批准机器 |
| 1 | T04-1 自动定期备份 | 学习数据持续保护和可见失败 | T02G 受控路径/manifest 基础；明确调度机制与停用证据 |
| 1 | T04-2 损坏与完整性检测 | 发现数据库或备份不可用，防止静默数据损失 | T04-1 的正式 backup 格式 |
| 1 | T05-1 全链路日志脱敏 | 真实 backup/restore 前先阻断秘密、资料原文、绝对路径和外部响应泄露 | T02F 日志安全底座；T04-1/2 的事件面 |
| 1 | T05-2 日志等级、事件与允许字段规范 | 真实 backup/restore 前使脱敏和诊断规则可执行、可审查 | T05-1 |
| 2 | T04-3 一键恢复与真实恢复演练 | 真实恢复写入、服务/计划任务停止、recovery point、状态机与回滚是数据安全核心 | T04-1、T04-2、T05-1、T05-2；批准的合成/目标机器 |
| 2 | T02-R3 真实备份/恢复安全复核 | 对 T04 的目标机写入前检查点和最终证据作独立安全签收；不能由实现者自证 | T04-1、T04-2、T05-1、T05-2；已审查的 T04-3 独立计划与 pre-write 证据（R3-prewrite 放行后才可真实写入） |
| 2 | T05-3 日志轮转与容量上限 | 防止长期运行日志无限增长；本轮到达容量时 fail-closed，不隐含 T05-4 保留/清理 | T05-1、T05-2 |
| 3 | T02-R4 学生核心流程失败反馈矩阵 | 面向用户的错误反馈不得泄露内部信息，且应可操作 | T02A/T02C；T05-1 可并行收口 |
| 4 | T02-R6 T02 总体回归与完成验收 | 只有已批准实际范围的 P0 已闭合、P1 全修复、Windows 证据和文档一致，才能关闭 T02 | Wave 0～3；既有 T02A～T02G |

### 明确不纳入本轮

- **T01-1、T01-2、T01-3**：S6 产品形态属于产品决策/条件变更，不是本轮安全与数据 P1 修复；继续保持“学生唯一操作者、家长只收异步脱敏报告、无家长登录/网页面板/公网入口”。
- **T02-R5**：其内容明确是 P2/P3 治理与剩余风险签收，保留至高权重工作稳定后单独排期。
- **T05-4**：保留期与安全清理仍是独立必做项，但不纳入本轮 11 项。T05-3 仅做轮转和容量上限；达到容量时必须以可见、安全失败停止新增日志，不得借“容量控制”删除历史文件、建立保留期或宣称完成安全清理。
- **T03-1～T03-3**：性能基线是必做但不是本轮 P1 数据/隐私恢复路径；在 Alpha/发布前仍需另行计划和验收。
- **T03-4、T02-P0、T01-3**：均为条件触发项，当前不触发。
- S3 Worker、完整 S7/G2、实时录音、ASR 网络试验、公网、多用户、家长 Web 面板、真实用户资料/音频进入测试夹具。

因此，本计划完成只代表“高权重 P1 恢复轨完成”；**不代表 Phase 3 整体完成**。Phase 3 仍至少保留 T01-1/2、T02-R5、T03-1/2/3 等必做未完成项。

---

## 2. 执行原则与安全门禁

1. 每一 Wave 都从最新 `master` 新建独立任务分支和独立 `.plans/` 实施计划；一个实施任务只覆盖一个可验收结果，避免巨型跨域变更。
2. 先写测试和验收矩阵，再写实现；后端接口优先使用真实 SQLite 集成测试，不 mock DB。
3. 所有会写数据的验证必须设置仓库外 `APP_DATA_ROOT`，使用唯一 task-id；不得触及真实学生数据。
4. T02-R2 的真实 Windows ACL 工作只允许只读采证，完成结果只能是脱敏事实、风险结论与是否需要最小修复的建议；不得把发现 P1 自动扩展为 ACL 写入。ACL 证据必须使用固定脱敏主体分类：当前交互用户、当前用户计划任务、SYSTEM、Administrators、其他本地用户、未知主体、deny ACE、继承状态、有效访问未知；不得记录完整用户名、完整 SID、ACL 原文或宿主路径。任何 `Set-Acl`、`icacls`、owner/DACL/SACL/继承变更均须另建最小修复任务，取得精确目标机器/逻辑目录的额外批准，并先具备当前 ACL 脱敏证据、最小授权矩阵、回滚和复验。不得改 Firewall、组策略、注册表或永久安全策略。
5. 真实恢复写入必须使用 fail-closed 状态机：用户对精确目标 Windows 机器、安装实例、逻辑 data 根和时间窗口再次批准；同一构建/同一安装实例的 Alpha D1 安装、检查、启动、健康、停止基础证据已通过；服务、相关计划任务和其他写入者均有“无 PID、无监听、无锁定/写入者”的脱敏停止证据；manifest/hash/允许路径/regular file/reparse/数量大小/磁盘空间/同卷与活动 data 根均预检通过；recovery point 在 payload 写入前创建并用清单/hash 验证。payload 必须先写入新的受控 staging 区并验证，随后才可按明确顺序切换；不得边验证边覆盖活动数据。未满足任一条件时保持 `RESTORE_WRITE_DISABLED`，不得绕过。
   - T04-3 独立实施计划必须在任何非 `-WhatIf` 写入前定义、实现并测试持久化、可审计的状态序列：`RESTORE_WRITE_DISABLED` → `PREWRITE_APPROVED` → `WRITERS_QUIESCED` → `PRECHECK_PASSED` → `RECOVERY_POINT_VERIFIED` → `STAGING_WRITTEN_AND_VERIFIED` → `CUTOVER_IN_PROGRESS` → `POST_RESTORE_VERIFICATION` → `RESTORE_COMPLETED`。状态记录和证据只可使用允许字段与脱敏摘要，不得记录绝对路径、原始 manifest、资料名或秘密。
   - 在每个不可原子阶段前必须写入受控、非活动 data 根中的脱敏中断标记，并明确切换写入顺序、每一步允许的前置状态、成功后的下一状态和失败出口；不得以“已复制部分 payload”推断活动数据安全。
   - 任一进程/主机重启、进程终止、磁盘或复制写入失败、hash 不符、状态记录缺失或无法确认状态，均必须转入 `RESTORE_RECOVERY_REQUIRED`。重启后的默认行为是保持所有写入者和服务停止、拒绝继续/重试/自动启动；仅经独立审查复核、人工确认并按受控处置路径才可进入 `ROLLBACK_IN_PROGRESS` → `ROLLBACK_VERIFIED`。recovery point 回滚或其验证失败必须进入最终 fail-closed 的 `RESTORE_MANUAL_ESCALATION`，不得恢复服务。
   - 只有 `RESTORE_COMPLETED` 或 `ROLLBACK_VERIFIED` 且 SQLite、manifest/hash、允许资料范围、关键只读 API/应用读取、保护哨兵和无越界变更均通过后，才可依照独立批准的启动步骤恢复服务。
6. T02-R1 的扫描输入仅限 Git 已跟踪文件清单和已获批准、已识别的候选部署包根；不得递归扫描整个工作区、用户目录或未知目录，且不得读取/枚举未跟踪路径 `H:\ai-studybuddy\.trae-html-share-packages\` 与 `H:\ai-studybuddy\alpha-sprint-plan\`。秘密扫描、日志、错误、截图和文档只保存规则名、经审核的程序相对路径或稳定文件 ID/逻辑类别、脱敏指纹、错误码、短哈希、耗时和摘要；对可能含姓名、资料标题或其他敏感信息的路径不得原样输出。绝不输出秘密值、原始资料、完整 UUID、绝对宿主路径、Provider 原始响应、原始终端 transcript、PowerShell 原始命令、ACL 原文/完整 SID、环境变量行或 manifest 原文。
7. T04-1 必须在独立实施计划中明确自动定期机制；如使用 Windows 计划任务，须验收任务身份、运行账户最小权限、动作路径、工作目录、环境输入、重复触发/并发互斥、失败可见性和禁用/停止方式；如不使用计划任务，须证明等效的停用、并发和恢复前置控制。T04-3 必须以可验证证据证明该自动写入机制已经停止，不能仅凭配置推断。
8. 每个任务完成前必须运行适用的 `pnpm type-check`、后端/前端 build、测试、文档治理、`git diff --check`；涉及页面/运行流的任务必须浏览器验收；合入 `master` 后必须复验并推送 `origin/master`。
9. 当前未跟踪路径 `H:\ai-studybuddy\.trae-html-share-packages\` 和 `H:\ai-studybuddy\alpha-sprint-plan\` 不属于本计划；不得删除、提交、移动或用 `git clean` 处理。

---

## 3. Wave 设计与完成门槛

### 跨 Wave 依赖、可并行与禁止并行

- **可并行**：T04-1/T04-2 的接口和事件定义稳定后，T05-1/T05-2 可并行实施与复验；T02-R4 可与 T05-3 并行，但均须遵守其各自独立计划和数据隔离要求。
- **禁止并行**：Wave 0 的 R1/R2 未完成实际证据与风险结论前，不得开始任何目标 Windows 机器真实 backup/restore 写入；R3-prewrite 必须在 T04-3 目标机写入前完成，不能与该写入并行；T05-1/T05-2 对 backup/restore 输出的脱敏/允许字段验收不得晚于该目标机写入；R3-postrestore 必须在演练终态或已验证回滚后进行。
- **最终顺序**：T02-R6 只能在 Wave 0～3 的适用证据、独立审查和文档事实均闭合后执行；不得以并行执行、单项合成证据或 Alpha 证据替代前序完成条件。

### Wave 0：正式证据基线（T02-R1、T02-R2）

**目的**：先在已批准范围内取得真实仓库/候选包和批准 Windows 机器的 P0/P1 风险证据；本 Wave 只读采证不等于 ACL 修复或真实操作授权，也不能因计划审查或未发现命中而宣称真实环境 `P0=0`。

- **T02-R1**：扫描输入仅为 Git 已跟踪文件清单和已获批准、已识别的候选部署包根；不得递归扫描整个工作区、未知目录或两条未跟踪路径。对每个候选部署包和可交叉引用的 Alpha/T04-3 证据，只记录非敏感 artifact ID、构建提交短哈希、包 SHA-256 短指纹和批准时间窗口，用于核对“扫描过的包、安装的包、演练使用的包”是否同一制品；不得记录绝对路径、资料名或完整 hash。命中只记录规则、经审核的程序相对路径或稳定文件 ID/逻辑类别、脱敏指纹和人工复核结论。现有 Alpha Day 0 扫描仅可作为输入；必须对当前正式仓库与当前候选包重新签收。
- **T02-R2**：只读采集 config/data/logs/backups/tmp/models 的 ACL、继承、固定脱敏主体分类、可写范围与 reparse 风险，输出脱敏风险结论及是否需要最小 ACL 修复的建议。发现 P1 时只能创建独立最小修复计划和回归矩阵；没有该修复任务的额外批准不得写 ACL。
- **Gate A（后续计划/合成验证）**：R1/R2 已在其实际采证范围内形成脱敏结论且尚无已确认 P0；每个 P1 均已有责任和最小修复路径；证据不泄露隐私。该 Gate 只允许后续计划与合成验证，不等于真实环境 `P0=0`。
- **Gate B（目标 Windows 机器真实 backup/restore 写入）**：相关 P1 必须已修复、自动化/目标机回归通过并由独立审查确认；“已有修复计划”不能放行。R2 一旦发现 data/config/backups ACL 宽泛、继承异常、未知主体、reparse 风险或访问模型不明，必须阻断任何真实备份和恢复写入。

### Wave 1：备份、完整性与真实恢复前日志门禁（T04-1、T04-2、T05-1、T05-2）

**目的**：先把 T02G 合成预检边界扩展为可验证的正式数据保护能力，并在任何目标机真实恢复写入前让日志脱敏和允许字段生效。

- **T04-1 自动备份**：只作用于明确受控输出根，白名单 payload、manifest/hash 和失败可见；独立实施计划必须明确自动定期机制，以及运行身份、最小权限、动作/工作目录/环境、重复触发与并发互斥、失败可见性、停用/停止证据。
- **T04-2 完整性检测**：覆盖全局库、学期库、备份 manifest/payload/hash，区分损坏、缺失、篡改和不可恢复。
- **T05-1、T05-2**：在 T04-3 目标机写入前覆盖 backup/restore 的控制台、脚本、服务日志、诊断和证据输出；定义日志等级、事件、关联短 ID、允许字段和禁用字段。恢复脚本临时输出不得规避该契约。
- **Gate**：备份不覆盖未知文件；篡改/损坏可识别；backup/restore 输出不会记录秘密、学生原文、完整 UUID、绝对路径或外部原始响应；自动备份写入者可被可靠停用并验证停止。

### Wave 2：受控恢复、独立复核与容量上限（T04-3、T02-R3、T05-3）

**目的**：在先有数据保护与日志门禁的前提下，完成可拒绝危险状态的恢复演练和独立安全复核；不把容量控制扩展为未纳入的保留/清理。

- **T04-3**：同一任务包含两个不可替代的验收门：先在批准的合成/隔离数据根完成受控写入演练，再通过独立 pre-write checkpoint 后才可进行经批准目标 Windows 机器真实写入演练。目标机写入前必须具备用户再次批准的精确机器、安装实例、逻辑 data 根和时间窗口；同一 artifact ID、构建提交短哈希、包 SHA-256 短指纹的 Alpha D1 基础证据通过；服务、计划任务与其他写入者均已停止；Gate B、R3-prewrite、预检和 recovery point 均已通过。独立实施计划必须落实本计划第 50 行的持久化状态机、中断标记、不可原子阶段、重启默认行为和回滚/人工升级出口。payload 先进入新的受控 staging 区并经验证，随后才按状态机允许的顺序切换；不满足任一状态转换条件、中断、写入失败、hash 不符或状态未知时均保持/进入 `RESTORE_RECOVERY_REQUIRED`，不得自动启动服务、继续写入或跳过回滚。回滚恢复已验证 recovery point 并复核保护哨兵；恢复后验证 SQLite、manifest/hash、允许资料范围、关键只读读取、重启健康与无越界变更。目标机真实写入不满足任一条件时保持 `RESTORE_WRITE_DISABLED`。
- **T02-R3**：同一任务含两个不可替代的独立门。**R3-prewrite** 由非 T04 实现者在任何目标 Windows 机器非 `-WhatIf` backup/restore 写入前，复核已审查的 T04-3 独立计划、精确目标机批准、Gate B、停止证据、状态机/中断标记、预检、recovery point 与日志允许字段契约；未签收则 T04-3 必须保持 `RESTORE_WRITE_DISABLED`。**R3-postrestore** 仅在演练结束、恢复后完整性或已验证回滚证据齐备后，独立复核受控根、白名单、manifest/hash、写入顺序、中断处理、回滚、恢复后完整性和无泄露证据；任一 P1 未闭合不得完成最终签收。
- **T05-3**：只验收轮转与容量上限。到达容量时必须以可见、安全失败停止新增日志；不得删除历史文件、配置保留期或宣称完成安全清理，T05-4 仍保持独立未完成。
- **Gate**：R3-prewrite 已在目标机真实写入前独立签收，真实 restore 具有受控写入、状态机/中断记录、回滚和恢复后完整性证据，且 R3-postrestore 独立复核无 P1 未闭合；重复启动、错误和轮转不无限增长，且不删除受保护或历史日志文件。

### Wave 3：学生可操作且脱敏的失败反馈（T02-R4）

**目的**：对 S1/S2/S3/S4/S5/S7 建立错误分类、API 契约、前端中文行动建议和回归矩阵。

- 覆盖输入错误、不存在、未配置、超时/下游失败和未知异常；前端不展示内部栈、路径、Provider 响应或秘密。
- S7 只验证其既定受控 MVP 边界；无合法 CLI/模型时输出已配置/待配置状态，而不是伪造成功。
- **Gate**：核心错误矩阵全绿，浏览器验收覆盖失败与重试建议；对既有成功路径无回归。

### Wave 4：T02 总体验收（T02-R6）

**目的**：关闭 T02 顶层，而非宣称整个 Phase 3 已完成。

- 汇总 T02A～T02G 与 R1～R4 的证据；仅在已批准实际范围的 P0 均已闭合、P1 全部修复、真实 Windows 原生证据和完整自动化回归通过后，才能报告 T02 完成。
- 对仍剩余 P2/P3 仅记录责任、缓解、触发和排期，不能伪装为已修复。
- **Gate**：独立审查通过；`docs/04` 和风险签收一致；主线复验并推送 `origin/master` 后才将 T02 标为完成。

---

## 4. 与 Alpha 的关系

- Alpha D1～D3（目标 Windows 安装、核心闭环、P0/P1 回归）继续有效，且可为 Wave 0、Wave 1、Wave 2、Wave 3 提供目标机证据。
- Alpha 不替代 Phase 3 验收；Phase 3 的 ACL、备份/恢复、日志和安全签收仍要按本计划独立完成。只有来自同一批准目标机、同一 artifact ID、构建提交短哈希、包 SHA-256 短指纹、同一逻辑目录类别、同一验收条件和同一批准时间窗口的脱敏证据才可交叉引用；引用只能避免重复采集，不能替代任一任务的完成标准。
- Alpha D1/D2/D3 不得被 R1、R2、T04 或 T05 自动勾选替代，反之亦然；每项任务仍须形成自己的完成证据。对 T04-3 而言，Alpha D1 的同构建安装、检查、启动、健康和停止结果只是目标机真实写入的操作前置条件，不是恢复演练、T02、Phase 3 或用户电脑验收完成证据。
- 如果 Alpha 发现 P0：立即停止普通 Wave，进入条件触发的 T02-P0 应急流程；不得继续普通实施。

---

## 5. 计划审查清单

独立审查必须确认：

- 11 项纳入任务确属必做且与 P1 安全、隐私、数据可恢复性或核心可用性门禁直接相关；排除项（含 T01、T02-R5、T03、T05-4 和条件触发项）不被误标为本轮或 Phase 3 已完成；
- 本次计划/文档审查未读取真实秘密、正式数据或目标 Windows 机器，故只能确认“尚无计划审查层面确认的 P0”，不能宣称真实环境 `P0=0`；R1/R2 的实际证据范围一旦确认真实 P0，必须停止普通 Wave 并转入条件触发的 T02-P0；T02-R6 只可基于已批准实际范围内 P0 已闭合的证据作结论；
- T04-3 同一任务严格按“批准的合成/隔离数据根写入演练 → R3-prewrite 独立 checkpoint → 经批准目标 Windows 机器真实写入演练 → R3-postrestore 最终独立复核”进行；目标机真实写入不在 Wave 0、未通过 Gate B、未签收 R3-prewrite、未获精确机器/安装实例/逻辑 data 根/时间窗口再次批准时发生；
- T04-3 独立实施计划在任何非 `-WhatIf` 写入前定义并测试持久化状态序列、每个允许转换、不可原子阶段前的中断标记、重启默认停止、`RESTORE_RECOVERY_REQUIRED` 的唯一人工处置/回滚路径及 `RESTORE_MANUAL_ESCALATION` 出口；只有 `RESTORE_COMPLETED` 或已验证的 `ROLLBACK_VERIFIED` 且完整性证据通过，才可按独立批准启动服务；
- T04-3 目标机 pre-write 证据包含同 artifact ID、构建提交短哈希、包 SHA-256 短指纹及批准时间窗口的 Alpha D1 前置，且包含停止证据、manifest/hash/允许路径/regular file/reparse/数量大小/容量/同卷/活动根预检和已验证 recovery point；恢复后 SQLite、manifest/hash、允许资料范围、关键只读 API/应用读取、重启健康、保护哨兵和无越界变更均有脱敏摘要；
- 真实 Windows ACL 的读证与写入修复严格分离：R2 完成结果仅为只读证据、固定脱敏主体分类（当前交互用户、当前用户计划任务、SYSTEM、Administrators、其他本地用户、未知主体、deny ACE、继承状态、有效访问未知）、风险结论和最小修复建议；任何 ACL 写入均须另建、另批、可回滚和复验的最小任务；
- T05-1/2 已在目标机真实恢复写入前对 backup/restore 的控制台、脚本、服务日志、诊断和证据输出生效，临时恢复输出不得规避允许字段契约；T05-3 只含轮转和容量上限，未隐含或替代未纳入本轮的 T05-4 保留期/安全清理；
- T04-1 的自动定期机制、运行身份、最小权限、动作/工作目录/环境、重复触发与并发互斥、失败可见性和可靠停用/停止证据可审查；如非计划任务机制，等效停用、并发与恢复前置控制同样可审查；
- Wave 可并行与禁止并行关系未被绕过：T05-1/2 可在 T04-1/2 接口/事件稳定后并行但不得晚于 T04-3 目标机写入；T02-R4 可与 T05-3 并行；R3-prewrite 不与真实写入并行，R3-postrestore 仅在演练终态或已验证回滚后进行，T02-R6 只能最后进行；
- R1 只扫描 Git 已跟踪清单和已批准、已识别候选包根，不递归扫描整个工作区；两条未跟踪路径不被扫描、读取、枚举、提交、移动、删除或清理；R1、Alpha D1 与 T04-3 的交叉引用只使用 artifact ID、构建提交短哈希、包 SHA-256 短指纹和批准时间窗口等非敏感制品字段；
- 秘密扫描、恢复、ACL、日志、错误、截图和证据不含 API Key、资料原文、完整 UUID、绝对路径、Provider 原始响应、敏感相对文件名、原始终端 transcript、PowerShell 原始命令、ACL 原文、完整 SID、环境变量行、manifest 原文或截图内绝对路径；
- Alpha 与 Phase 3 不互相篡改验收口径，且仅同一批准目标机、同一制品、同一逻辑目录类别、同一验收条件和同一批准时间窗口的脱敏证据可交叉引用；Alpha D1/D2/D3 不替代任一 Phase 3 任务验收。

审查通过后，请用户明确回复：`批准按 PHASE3-REOPEN-HIGH-WEIGHT-20260728 从 Wave 0 开始实施`。只有收到该确认，才创建 T02-R1/T02-R2 的实施计划和实现分支。
