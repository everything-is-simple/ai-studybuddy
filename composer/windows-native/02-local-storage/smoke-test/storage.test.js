const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('node:fs'); const path=require('node:path'); const os=require('node:os'); const s=require('../../shared/storage');
test('local storage writes, reads, deletes and rejects traversal',()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'studybuddy-store-'));const key='materials/course-1/2026-05-31/sample.txt';s.writeFile(root,key,'中文资料');assert.equal(s.readFile(root,key).toString(),'中文资料');assert.throws(()=>s.writeFile(root,key,'duplicate'));assert.throws(()=>s.resolveStorageKey(root,'../secret.txt'));assert.throws(()=>s.resolveStorageKey(root,path.resolve(root,'secret.txt')));s.writeFile(root,'tmp/job-1/temp.txt','temp');s.cleanTmp(root);assert.equal(fs.existsSync(path.join(root,'tmp')),false);assert.equal(s.readFile(root,key).toString(),'中文资料');s.deleteFile(root,key);assert.equal(fs.existsSync(path.join(root,key)),false);});
